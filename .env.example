# --- App ---
PROJECT_NAME="Smart Library Management Backend"
API_V1_PREFIX="/api/v1"
ENVIRONMENT="local"  # local | staging | production
BACKEND_CORS_ORIGINS=["http://localhost:5173"]

# --- Security ---
# Tạo giá trị mới bằng: openssl rand -hex 32
# Hãy giữ nguyên giá trị xử lý auth không bị lỗi
SECRET_KEY="8e9bb5f6bac0610afbd1096c6eaf883537194d7a12176051bdcf9de28df5d502"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7
# false khi chay local qua http; PHAI true khi deploy production qua https
# (khong thi browser se khong gui cookie refresh_token di)
COOKIE_SECURE=false

# --- Database ---
DATABASE_URL=""
