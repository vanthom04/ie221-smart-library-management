Bật extension pgvector, chạy lệnh:
CREATE EXTENSION IF NOT EXISTS vector;

Chạy Migration:
Thêm bảng book_embeddings vào database, dùng alembic/versions/20260301_0001_create_book_embeddings.py.
***Nhớ sửa revision: str = "20260301_0001", thành phiên bản mới nhất trong codebase.
Sau đó chạy lệnh: 
alembic upgrade head

Tạo index HNSW để tối ưu tốc độ tìm kiếm vector, chạy lệnh:
CREATE INDEX IF NOT EXISTS ix_book_embeddings_hnsw 
ON book_embeddings USING hnsw (embedding vector_cosine_ops);